#include <stdio.h>
/**
 * main: Entry point
 * @argc: number of arguments
 * @argv: array of strings
 * Return: 0
 */
int main(int __attribute__ ((unused))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
