#include <stdio.h>
/**
 * main - Entry point
 * @argc: number of arguments
 * @argv: array of strings
 * Return: 0
 */
int main(int argc, char __attribute__ ((unused))*argv[])
{
	printf("%d\n", argc);
	return (0);
}
